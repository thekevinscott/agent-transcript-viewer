target/
node_modules/
dist/
coverage/
*.log
.DS_Store
.pnpm-store/

# Agent worktrees (see AGENTS.md)
.worktrees/

# Local agent planning notes (issue drafts, session state)
internal/
.github/internal/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/

# viewer.html copied in from packages/node/dist by the Python build hook
# (packages/python/hatch_build.py) — never committed, only built.
packages/python/src/agent_transcript_viewer/_assets/

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads issue tracker (local only)
.beads/
docs/.vitepress/cache
docs/.vitepress/dist
docs/node_modules/

# Stryker scratch files, written by the mutation gate
.stryker-tmp/
stryker-setup-*.js

# Playwright run artifacts
packages/node/test-results/
