[python]
# The package is a version stub — no conditionals anywhere in it — and the
# branch gate hard-fails when a report measures zero branches. Restore the
# default (`branch = true`) as soon as real code lands.
coverage = { branch = false }

[[python.exempt]]
path = "hatch_build.py"
rules = ["colocated-test"]
reason = "hatchling build-backend plugin, loaded by path at build time and never importable from the package; its behaviour is covered end-to-end by tests/integration/viewer_asset_packaging_test.py, which builds the real wheel and sdist"
