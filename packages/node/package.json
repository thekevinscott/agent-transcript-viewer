{
  "name": "agent-transcript-viewer-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Internal frontend workspace for the agent-transcript-viewer static HTML viewer. Never published; the built artifact ships inside the Python wheel.",
  "license": "MIT",
  "repository": "https://github.com/thekevinscott/agent-transcript-viewer",
  "type": "module",
  "files": [
    "dist"
  ],
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "^22.9.0",
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.61.0",
    "@vitest/coverage-v8": "^3.2.0",
    "eslint": "^10.4.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
